# Every algorithm named, assigned to a mechanism, and run

Round 42. A coverage claim is worth its counterexamples: here is the list, and here is each implementation actually factoring something.

## The list

| algorithm | year | mechanism | where it lives here | factored its test number | time |
|---|---|---|---|---|---|
| trial division | antiquity | size | `arith.spf_trial` | yes | 0.00 s |
| Pascal row residue scan (T1-T5) | this project | size | `factor.pascal_split` | yes | 0.00 s |
| Pollard-Strassen product tree | 1974/76 | size | `fast.fast_split` | yes | 0.08 s |
| factorial threshold search (T20) | this project | size | `fast.threshold_spf` | yes | 0.03 s |
| Fermat / Lehman | 1643/1974 | size | in `harvey.lehman_recover` | -- | -- |
| Harvey's deterministic `N^(1/5)` | 2021 | size | `harvey.harvey_factor` | yes | 0.04 s |
| hyperbola hull walk (P23) | this project | size | `hyperbola.hyperbola_factor` | yes | 0.00 s |
| binary search by divisor count | this project | size | `hyperbola.binary_search_factor` | yes | 2.10 s |
| Coppersmith, given `p` to within about `N^(1/4)` | 1996 | size | `lattice.factor_with_hint` | yes | 0.01 s |
| Pollard rho | 1975 | collision | `arith.pollard_rho` | yes | 0.00 s |
| Pascal rho `x -> C(x,k)` (P22) | this project | collision | `collision.pascal_rho` | yes | 0.00 s |
| Pollard `p - 1` | 1974 | order | `cyclo.pollard_pminus1` | yes | 0.00 s |
| Williams `p + 1` | 1982 | order | `cyclo.williams_pplus1` | yes | 0.00 s |
| Schnorr-Lenstra class groups | 1984 | order | `classgroup.schnorr_lenstra_split`, which returns `(factor, multiplier)` | yes | 0.00 s |
| Lenstra's ECM | 1987 | order | `ecm.ecm` | yes | 0.02 s |
| elliptic triangle, row `lcm(1..B)` (P24) | this project | order | `divseq.elliptic_triangle_factor` | yes | 0.02 s |
| Shor | 1994 | order (read, not guessed) | `shor.shor` | yes | 0.00 s |
| central column Legendre symbols | this project | sign | `central.central_split` | yes | 0.04 s |
| Shanks' SQUFOF | 1975 | sign | `squfof.squfof` | yes | 0.00 s |
| CFRAC | 1931/1975 | sign | `cfrac.cfrac` | yes | 0.00 s |
| Dixon's random squares | 1981 | sign | unsieved CFRAC/QS | -- | -- |
| quadratic sieve | 1981 | sign | `qs.quadratic_sieve` | yes | 0.01 s |
| MPQS / SIQS | 1987 | sign | engineering on top of the QS | -- | -- |
| number field sieve | 1993 | sign | `nfs.number_field_sieve` | yes | 0.26 s |
| Schnorr's prime-number lattice | 1991 | sign | `schnorr.split_from_relations` | -- | -- |
| Regev's quantum variant | 2023 | order (read) | not simulable here | -- | -- |

21 of 21 implementations returned a value that is a nontrivial divisor of the number they were given. The instances differ by row on purpose: `p - 1` and `p + 1` are handed a prime whose shifted value is smooth, Shor is handed 21 because its simulator is a state vector, and the methods with large constants are handed 24 bits rather than 40. This table is a coverage check, not a race -- round 27 is the race.

## The rows with no implementation, and why

| algorithm | why it is not here |
|---|---|
| Fermat / Lehman | Lehman's certificates are produced by the hull walk (P23) and consumed by `harvey.lehman_recover`; Fermat is the `k = 1` case |
| Dixon's random squares | the quadratic sieve with the sieve removed -- same mechanism, strictly more work |
| MPQS / SIQS | polynomial switching and self-initialisation: better constants for the same `L[1/2, 1]` |
| Schnorr's prime-number lattice | the lattice and its relations are implemented (round 20); it never produced a usable relation, which is round 20's result |
| Regev's variant | quantum, and its circuit is not a state vector this machine can hold |


## What would falsify the taxonomy

A method that produces a nontrivial `gcd(x, N)` without `x` being a difference of two things congruent mod `p` (collision, sign), a candidate divisor or a batch of them (size), or a group element killed by an exponent (order). Nothing in the list above is such a method, and the two additions this project made -- the hyperbola hull walk and the elliptic triangle -- landed in size and order respectively rather than anywhere new.

The nearest thing to an escape is not a mechanism at all but a change of question. Implicit factoring (round 37) does not attack one `N`; it attacks `k` of them that share bits, and it is the one entry here whose input is not a single integer:

| algorithm | year | input | mechanism | factored | time |
|---|---|---|---|---|---|
| implicit factoring (May-Ritzenhofen) | 2009 | 3 moduli sharing 100 low bits of `p` | lattice, between instances | yes | 0.00 s |

Which is why round 37 is filed as a different currency rather than a fifth mechanism: it buys its zero divisor from a correlation between instances, and there is no correlation inside one instance to buy.
_Generated by `experiments/exp48_taxonomy.py` in 2.6s._
